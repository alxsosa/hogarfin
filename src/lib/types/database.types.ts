/**
 * Placeholder Supabase database types.
 *
 * Once the Supabase project is linked (via the Vercel Marketplace
 * integration or `supabase link`), regenerate this file with:
 *
 *   supabase gen types typescript --linked > src/lib/types/database.types.ts
 *
 * Until then this is an untyped escape hatch so the rest of the app
 * can compile against `Database` without blocking on provisioning.
 */
// eslint-disable-next-line @typescript-eslint/no-explicit-any
export type Database = any;
